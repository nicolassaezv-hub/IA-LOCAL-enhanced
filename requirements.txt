# ASTRA — requirements.txt
# Linux/Replit-compatible. Windows-only packages (pywin32, comtypes, dearpygui,
# mss, PyAutoGUI, etc.) are intentionally excluded.
# Heavy optional packages (torch, tensorflow, librosa…) are listed at the bottom
# as comments — install manually: pip install <package>

# ── AI / API client (REQUIRED — used for Groq and optional OpenAI) ───────────
openai>=1.0
groq>=0.11.0

# ── Workspace web (REQUIRED — FastAPI SPA server) ─────────────────────────────
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
pydantic>=2.0

# ── Core data & file I/O ─────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
openpyxl>=3.1
PyPDF2>=3.0
python-docx>=1.0
pdfplumber>=0.9
PyYAML>=6.0

# ── Web & scraping ────────────────────────────────────────────────────────────
requests>=2.30
beautifulsoup4>=4.12
lxml>=4.9
deep-translator>=1.11

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13
Pillow>=10.0
reportlab>=4.0
rich>=13.0
tabulate>=0.9
colorama>=0.4

# ── ML & analytics ───────────────────────────────────────────────────────────
scikit-learn>=1.3
scipy>=1.11
xgboost>=2.0
lightgbm>=4.3
optuna>=3.6
faiss-cpu>=1.7
joblib>=1.3
yfinance>=0.2   # descarga Yahoo Finance para 'generar csvs forex' y auto-updater
imbalanced-learn>=1.2  # SMOTE en xgb_trainer.py y prediction_lab/validation_engine.py
shap>=0.44           # feature importance en forex/prediction/feature_importance.py

# ── NLP ──────────────────────────────────────────────────────────────────────
nltk>=3.8            # tokenización, sentimiento, POS tagging en nlp_tools.py
regex>=2024.0        # regex avanzado en nlp_tools.py

# ── Database & storage ────────────────────────────────────────────────────────
SQLAlchemy>=2.0

# ── Security ──────────────────────────────────────────────────────────────────
cryptography>=42.0
bcrypt>=4.0
PyJWT>=2.8
paramiko>=3.0
passlib>=1.7

# ── Utilities ─────────────────────────────────────────────────────────────────
psutil>=5.9
tqdm>=4.65
schedule>=1.2
arrow>=1.3
orjson>=3.9
filelock>=3.13
watchdog>=4.0
sympy>=1.12

# ── Optional — install manually with pip if needed ────────────────────────────
# scikit-image>=0.21     — Image processing
# torch>=2.0             — PyTorch deep learning
# torchvision>=0.17      — PyTorch vision models
# torchaudio>=2.0        — PyTorch audio models
# tensorflow>=2.14       — TensorFlow deep learning
# keras>=3.0             — Keras deep learning API
# h5py>=3.10             — HDF5 matrix file handling for deep learning
# librosa>=0.10          — Audio analysis (requires soundfile, resampy)
# pydub>=0.25            — Audio conversion (requires ffmpeg)
# sounddevice>=0.4       — Audio playback (requires PortAudio)
# SpeechRecognition>=3.10 — Voice recognition (requires mic)
# pyttsx3>=2.90          — Text-to-speech (requires audio device)
# pytube>=15.0.0         — YouTube video/audio downloading
# imageio>=2.34          — Image and animated GIF/video frame read/write
# llama-index>=0.10      — LlamaIndex RAG framework
# redis>=5.0             — Redis client
# flask>=3.0             — Flask web framework
# grpcio>=1.60           — gRPC
# aiohttp>=3.9           — Async HTTP server/client
# python-socketio>=5.0   — Socket.IO
# websockets>=12.0       — Async WebSocket client/server
# websocket-client>=1.7  — Synchronous WebSocket client
# pytest>=8.0            — Test runner (for test_main.py)
#
# ── Cloud SDKs (optional — install only if using cloud_tools.py) ──────────────
# azure-storage-blob>=12.19  — Azure Blob Storage
# azure-identity>=1.15       — Azure authentication
# google-cloud-storage>=2.14 — Google Cloud Storage
# boto3>=1.34                 — AWS S3 / Cloud resources
#
# ── Windows-only packages (not installable on Linux/Replit) ───────────────────
# MetaTrader5>=5.0.45       — MetaTrader 5 live trading (Windows only)
# PyQt5>=5.15               — Qt desktop GUI
# dearpygui>=1.11          — GPU-accelerated Python GUI
# kivy>=2.3                — Cross-platform touch GUI
# pyautogui>=0.9           — Desktop GUI automation
# pygetwindow>=0.0.9       — Window management
# pytesseract>=0.3         — OCR engine wrapper
# mss>=9.0                 — Fast screenshots
# keyboard>=0.13           — Global keyboard hook
# mouse>=0.5              — Global mouse hook
# keyring>=24.0            — OS secure password storage
#
# ── Dev utilities ─────────────────────────────────────────────────────────────
httpx>=0.27.0
python-dotenv>=1.0.0
