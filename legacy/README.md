# legacy/

`astra_legacy_v1.py` es la primera versión de ASTRA (usa la API legacy de
OpenAI y gpt-3.5-turbo). Fue reemplazada por `main.py` + `astra_agent.py`
(Groq/Llama-3.3-70B). Se conserva solo como referencia histórica — no se
importa desde ningún otro módulo. Se puede borrar sin riesgo si no la
necesitas.
