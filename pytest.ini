[pytest]
addopts = -m "not external"
markers =
    unit: isolated test with no external service or production artifact access
    integration: opt-in multi-component test with all writable resources isolated
    external: opt-in test that requires a configured external service
