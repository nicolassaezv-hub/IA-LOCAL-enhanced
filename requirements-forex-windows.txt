# OPTIONAL WINDOWS FOREX — never installed by the Linux production requirements.
# Requires Windows and an installed, already authenticated MetaTrader 5 terminal.
MetaTrader5==5.0.6147; platform_system == "Windows"
